import IntegrityPage from "@/components/integrity/IntegrityPage";

export const metadata = {
  title: "Payroll & HR",
  description: "Payroll services, HR resources, payroll tax services, time and attendance, workers comp, employee benefits, and retirement plans.",
};

export default function Page() {
  return <IntegrityPage slug="payroll" />;
}
